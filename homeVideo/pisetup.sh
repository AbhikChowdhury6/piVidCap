# manually
# install git
# clone repo

#in this script
#prompt for device name
#prompt for remote username
# prompt for remote password

#install video capture dependencies
# ultralytics (opencv installs with it I think)
# pandas
    # pip install pyarrow
    # pip install fastparquet
    # pip install tzlocal